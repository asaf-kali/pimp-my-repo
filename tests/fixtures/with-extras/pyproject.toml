[project]
name = "with-extras"
version = "0.1.0"
requires-python = ">=3.9"

[project.optional-dependencies]
dev = ["ruff>=0.8.0", "mypy>=1.0"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.ruff.lint]
select = ["E", "F"]

[tool.mypy]
ignore_missing_imports = true

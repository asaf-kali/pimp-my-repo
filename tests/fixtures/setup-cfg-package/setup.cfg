[metadata]
name = my-pkg
version = attr: my_pkg.__version__

[options]
packages = find:
python_requires = >=3.9
install_requires =
    iniconfig

[options.entry_points]
console_scripts =
    my-pkg = my_pkg.main:greet
